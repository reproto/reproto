extern crate reproto_core as core;
extern crate genco;

#[derive(Debug)]
pub struct Generator {
}

impl Generator {
    /// Constructs a new generator.
    pub fn new() -> Self {
        Self {
        }
    }
}

package foo.bar.baz;

// hello

message Foo {
    reserved "foo";

    required string name;
}

interface Bar {
    reserved "a", "b", "c";

    A {
        name "foo";
        required string name;
    }
}

package foo.bar.baz;

use foo.bar;
use foo as bar2;

type Tuple = (double, double);

// hello

message Foo {
    reserved "foo";

    name: string;
}

/**
 * Hello World.
 */

interface Bar {
    reserved "a", "b", "c";

    A {
        name "foo";
        name: string;
    }
}

type ArrayOfTuples = [(double, double)];

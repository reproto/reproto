package foo.bar.baz;

type Tuple = (double, double);

// hello

message Foo {
    reserved "foo";

    required string name;
}

interface Bar {
    reserved "a", "b", "c";

    A {
        name "foo";
        required string name;
    }
}

type Array = [ (double, double) ];

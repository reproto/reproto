package foo.bar.baz;

use foo.bar;
use foo as bar2;

tuple Tuple {
  arg: double;
  second: double;
}

// hello

type Foo {
  reserved "foo";

  name: string;
  map: {string: unsigned/32};

  java {{
    public int hello() {
      return 42;
    }
  }}
}

/**
 * Hello World.
 */

interface Bar {
  reserved "a", "b", "c";

  java {{
    default int hello() {
      return 42;
    }
  }}

  A {
      name "foo";
      name: string;
  }
}

enum Baz {
  THIRD(2, "third");
}

// Support type aliases?
// type ArrayOfTuples = [Tuple];

package foo.bar.baz;

type Tuple = (double, double);

// hello

message Foo {
    reserved "foo";

    name: string;
}

interface Bar {
    reserved "a", "b", "c";

    A {
        name "foo";
        name: string;
    }
}

type ArrayOfTuples = [(double, double)];

package foo.bar.baz;

use foo.bar;
use foo as bar2;

type Tuple = (double, double);

// hello

message Foo {
    reserved "foo";

    name: string;
    map: {string: u32};

    java @@
      public int hello() {
        return 42;
      }
    @@
}

/**
 * Hello World.
 */

interface Bar {
    reserved "a", "b", "c";

    java @@
      default int hello() {
        return 42;
      }
    @@

    A {
        name "foo";
        name: string;
    }
}

type ArrayOfTuples = [(double, double)];

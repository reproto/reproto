package test;

type Entry {
  name: string;
}

service Example {
  endpoint {
    path "/query";

    // # text-based endpoint
    endpoint {
      consumes "text/plain";
    }

    // # json-based endpoint
    endpoint {
      consumes "application/json";
    }

    response ErrorResponse {
      status 500;
      produces "application/json";
    }
  }
}

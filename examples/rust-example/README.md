# Example Rust Project using Reproto

Rebuild generated files:

```bash
cargo run --manifest-path=../../cli/Cargo.toml -- build
```

Run with:

```bash
cargo run
```

This should list all the public gists for the user `udoprog`.

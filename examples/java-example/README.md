# Example Java Project using Reproto

Rebuild generated files:

```bash
reproto build
```

Run with:

```bash
mvn exec:java -D exec.mainClass="io.reproto.example.Full"
```

This should list all the public gists for the user `udoprog`.

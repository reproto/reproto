package heroic;

type Point = (i64, double);
type Event = (i64, any);

interface Samples {
  Points {
    // default: heroic/Samples.Points
    name "points";

    required Point[] data;
  }

  Events {
    // default: heroic/Samples.Events
    name "events";

    required Event[] data;
  }
}

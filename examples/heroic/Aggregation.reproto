package heroic;

interface Aggregation {
  Sum {
    // default: heroic/Aggregation.Sum
    name "sum";

    sampling?: Sampling;
    size?: Duration;
    extent?: Duration;
  }

  Average {
    // default: heroic/Aggregation.Average
    name "average";

    sampling?: Sampling;
    size?: Duration;
    extent?: Duration;
  }
}
